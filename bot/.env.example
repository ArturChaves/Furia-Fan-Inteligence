# Exemplo de configuração para o Bot FURIA Fan Intelligence

# URL de conexão com o banco de dados PostgreSQL
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Configurações de autenticação RabbitMQ (se usar no Bot futuramente)
RABBITMQ_URL="amqp://USER:PASSWORD@HOST:PORT"

# (Opcional) Chave JWT, caso o Bot precise gerar tokens para autenticação no futuro
JWT_SECRET="sua_chave_jwt_segura"

# Outras variáveis que forem necessárias podem ser adicionadas aqui
